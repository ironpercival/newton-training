Используем тематику предыдущего задания. 


1. Добавьте как минимум 5 новых сущностей (по аналогии с Person, FootballClub, Company), по 3-4 свойства в каждой сущности.

2. При помощи атрибутов укажите для каждой сущности регулярное выражение, которым можно сущность распарсить, например

[Regex(@"(Mr\.|Mrs\.|Ms\.)\s(\w+)\s(\w+)\swas\sborn\son\s(\d{4}/\d\d/\d\d)")]
public class Person { 
...
}

3. Измените программу так, чтобы регулярное выражение считывалось из аттрибута и использовалось в парсинге 

4. Реализуйте метод, который возвращает коллекцию всех свойств всех сущностей и их типы (без повторений), например:
[<Name, string>, <BirthDate, DateTime>, <Country, string> ... ]

5. Выведите, сколько раз в свойствах ваших сущностей используется каждый тип данных, в порядке убывания, например:
string - 11
int - 5
DateTime - 3

6. Задание выполнить в виде .NET Core Console App.